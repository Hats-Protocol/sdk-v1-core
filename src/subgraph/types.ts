type GqlHat = {
  id: string;
  status: boolean;
  details: string;
  maxSupply: number;
  currentSupply: number;
  eligibility: string;
  toggle: string;
  mutable: boolean;
};

export type { GqlHat };
